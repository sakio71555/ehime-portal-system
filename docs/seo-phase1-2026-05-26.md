# SEO改善 第1フェーズ メモ

作成日: 2026-05-26

## 目的

Google Search Console 対策として、インデックスさせるべきページとインデックスさせるべきではないページを整理した。
特に、任意検索結果 URL が検索結果に出る問題を抑えつつ、補助金詳細ページの検索結果表示品質と信頼性表示を強化する。

## 実施した変更

- `/search?keyword=...` ページに `noindex,follow` を出すようにした。
- 静的な `index.html` に残っていた固定 `robots` / description / OGP / Twitter meta を外し、React 側の SEO コンポーネントに統一した。
- WebSite 構造化データから SearchAction を削除した。
- sitemap 生成から `/search` と `/search?keyword=...` 系 URL を除外した。
- 補助金詳細ページの title / meta description を補助金ごとに個別生成するようにした。
- 補助金詳細ページに、申請前に確認すべき公式情報ボックスを追加した。

## SEO上の判断

### `/search?keyword=...` の扱い

検索結果ページは、ユーザーには便利な内部導線だが、検索エンジン向けには重複・低品質ページになりやすい。
そのため、robots.txt ではブロックせず、HTML head に `noindex,follow` を出す方針にした。

robots.txt で `/search` を `Disallow` すると、Google がページ内の `noindex` を確認できない可能性があるため、今回はブロックしない。

### `/search?keyword={search_term_string}` の原因と修正

原因は WebSite 構造化データの SearchAction target に `{search_term_string}` を含む URL テンプレートが出ていたこと。
実リンクとしてクロールされるリスクを避けるため、今回は SearchAction 自体を削除した。

WebSite / BreadcrumbList / GovernmentService など、他の構造化データは維持する。

### sitemap除外ルール

sitemap には canonical として見せたい固定URLだけを含める。
以下は除外する。

- `/search`
- `/search?keyword=...`
- `/search?keyword={search_term_string}`
- 任意検索結果やパラメータ付き検索URL

sitemap に残す主なURLは以下。

- `/`
- `/features`
- `/feature/...`
- `/subsidy/{id}`
- `/columns`
- `/column/{slug}`
- `/expert-articles`
- `/expert-articles/{slug}`
- 固定のSEOランディングページ

## 補助金詳細ページのtitle / description生成ルール

title:

```text
{補助金名}｜{申請期間・補助上限・対象者など存在する項目}｜愛媛の補助金
```

description:

```text
{補助金名}の対象者、補助上限額、申請期間、公募状況、公式情報を整理。{地域名}の事業者・個人向けに要点を確認できます。申請前に必ず公式情報をご確認ください。
```

値がない項目は自然に省略し、`undefined` / `null` / `未確認` などを検索結果向け文言に出さない。

## 補助金詳細ページに追加した信頼性表示

詳細ページ上部に「申請前に確認したい公式情報」ボックスを追加した。

表示項目:

- 公募ステータス
- 申請期間
- 受付開始日
- 受付終了日
- 実施機関
- 対象地域
- 対象者
- 補助上限額
- 補助率
- 公式URL
- 公式情報確認日
- 注意書き: 掲載情報は制度探しの入口であり、申請前に必ず公式ページを確認する

DBに値がない項目は空欄表示を避ける。公式情報確認日は値がない場合 `未確認` として扱う。

## 確認結果

実行コマンド:

```bash
node --check ehime-hojo-app/scripts/generate-sitemap.mjs
npm --prefix ehime-hojo-app run generate:sitemap
npm --prefix ehime-hojo-app run build
npm --prefix ehime-hojo-app run lint
```

結果:

- `node --check`: 成功
- `generate:sitemap`: 成功
- `build`: 成功
- `lint`: 既存のバックアップファイルや別作業中ファイルの lint error により失敗

sitemap 確認:

- `/search?keyword=...`: 0件
- `{search_term_string}`: 0件
- `/features`: あり
- `/feature/...`: あり
- `/subsidy/1570`: あり
- `/subsidy/1219`: なし
- `<url>` 件数: 464件

ブラウザ確認:

- `/search?keyword=松山市`: `noindex,follow`
- `/search?keyword=製造業`: `noindex,follow`
- `/subsidy/1570`: `index,follow`、個別title / description
- `/columns`: `index,follow`
- `/`: `index,follow`
- HTML内に `{search_term_string}` が実リンクとして出ていないことを確認

### 追加確認: 初期HTMLとJS実行後DOM

2026-05-26 に、ビルド成果物をローカルHTTPサーバーで配信し、初期HTMLとChrome headlessによるJS実行後DOMを確認した。

確認対象:

- `/search?keyword=松山市`
- `/subsidy/1570`

結果:

- `/search?keyword=松山市` の初期HTMLには、SPAの仕様上、動的な `robots` / description / canonical は出ない。
- `/search?keyword=松山市` のJS実行後DOMには `<meta name="robots" content="noindex,follow">` が出る。
- `/search?keyword=松山市` のJS実行後DOMには、検索語に応じた title / description / canonical が出る。
- `/subsidy/1570` の初期HTMLには、SPAの仕様上、動的な title / description / canonical は出ない。
- `/subsidy/1570` のJS実行後DOMには、補助金ごとの個別 title / description / canonical が出る。
- `/subsidy/1570` のJS実行後DOMは `index,follow` のままで、詳細ページに不要な `noindex` は出ない。
- 初期HTMLとJS実行後DOMのどちらにも `{search_term_string}` は出ない。

確認結果の例:

```text
/search?keyword=松山市 JS実行後:
robots: noindex,follow
title: 松山市に関連する補助金・助成金を検索｜愛媛の補助金・助成金ポータル

/subsidy/1570 JS実行後:
robots: index,follow
title: 中小企業者収益力強化補助金｜申請期間・補助上限・対象者｜愛媛の補助金
description: 中小企業者収益力強化補助金の対象者・補助上限額：上限200万円・申請期間：2026年5月29日締切・公募状況：公募中、公式情報を整理。愛媛県内の事業者・個人向けに要点を確認できます。申請前に必ず公式情報をご確認ください。
```

重要な設計メモ:

- 現状のフロントはReact SPAのため、GooglebotがJavaScriptを実行した後に動的metaを確認する設計になっている。
- Search Console の URL検査で `/search?keyword=松山市` の「公開URLをテスト」を行う場合、レンダリング後HTMLで `noindex,follow` が検出される想定。
- ただし、初期HTMLだけを見るクローラーや一部ツールでは動的metaが見えない可能性がある。

### X-Robots-Tag の検討

`/search?keyword=...` の `noindex` をより確実にするには、本番配信側で `/search` にだけHTTPヘッダーを追加する方法が有効。

nginx設定案:

```nginx
location = /search {
    add_header X-Robots-Tag "noindex, follow" always;
    try_files $uri /index.html;
}
```

注意:

- `/`, `/subsidy/...`, `/column/...`, `/features`, `/feature/...` など、インデックスさせたいページには絶対に付けない。
- robots.txt で `/search` を `Disallow` しない方針は維持する。
- 今回はリポジトリ内に本番nginx設定を管理していないため、コード変更としては未実装。必要ならVPS側のnginx設定で別途対応する。

## 未対応事項

- Search Console で古い `/search?keyword=...` URL がインデックス済みの場合、noindex認識まで時間がかかる。
- `published_at` や `official_checked_at` などの確認日系カラムがデータごとに十分埋まっていない可能性がある。
- 補助金詳細の構造化データは今後 `dateModified` やより正確な `additionalProperty` を増やせる。
- lint は既存のバックアップファイルや別作業差分で失敗しているため、別途整理が必要。
- `/search?keyword=...` の `noindex` はJS実行後DOMで出る。必要に応じてVPSのnginxで `X-Robots-Tag: noindex, follow` を追加すると、より確実にGoogleへ伝えられる。

## 次フェーズの静的LP案

今後は検索結果URLではなく、固定LPとして以下を整備する。

### 地域別

- `/area/matsuyama/`
- `/area/imabari/`
- `/area/uwajima/`
- `/area/seiyo/`

### 業種別

- `/industry/manufacturing/`
- `/industry/restaurant-retail/`
- `/industry/tourism/`

### 目的別

- `/purpose/startup/`
- `/purpose/dx/`
- `/purpose/energy-saving/`

### 状態別

- `/status/open/`

各LPに必要な内容:

- 固有title
- 固有meta description
- 対象補助金一覧
- 200〜500字程度の説明文
- 関連する市町村・業種・目的への内部リンク
- 補助金詳細ページへのHTMLリンク
- 公式情報確認の注意書き

## 2026-05-26 本番反映ログ

### 反映コミット

- `21d810e Improve SEO indexing controls and subsidy metadata`
- VPS上では `3fbb158` から `21d810e` へ fast-forward したため、途中の `56f62f2 Add expert article delete and image style controls` も同時に本番へ反映された。
- ローカル作業ツリーには管理画面・UI・バナー画像などの別件未コミット差分が残っていたが、今回の本番反映はGitHub `origin/main` のコミット済み内容のみをVPSでpullして実施した。

### 実行した本番反映手順

```bash
ssh -i ~/.ssh/id_ed25519 root@160.251.174.201

cd /opt/ehime-portal-system
git fetch origin
git pull --ff-only origin main
git log --oneline -3

cd /opt/ehime-portal-system/ehime-hojo-app
npm run build

BACKUP=/root/ehime-hojokin-www-backup-$(date +%Y%m%d-%H%M%S).tar.gz
tar -czf "$BACKUP" -C /var/www ehime-portal

rsync -a --delete dist/ /var/www/ehime-portal/
```

### バックアップ

- `/root/ehime-hojokin-www-backup-20260526-192503.tar.gz`

### build結果

```text
npm run build
✅ sitemap.xml 生成完了
静的ページ: 26 件
補助金詳細: 418 件
コラム詳細: 19 件
専門家記事詳細: 1 件
合計: 464 件
vite build: 成功
```

Viteのchunk size warningのみ。ビルド失敗はなし。

### 本番確認結果

確認はBrowser / Chrome / Playwright / Puppeteerを使わず、`curl`、`grep`、VPS上の配置ファイル確認で実施した。

#### HTTP確認

- `https://ehime-hojokin.jp/`: HTTP 200
- `https://ehime-hojokin.jp/search?keyword=松山市`: HTTP 200
- `https://ehime-hojokin.jp/search?keyword=製造業`: HTTP 200
- `https://ehime-hojokin.jp/subsidy/1570`: HTTP 200
- `https://ehime-hojokin.jp/columns`: HTTP 200
- `https://ehime-hojokin.jp/sitemap.xml`: HTTP 200

#### sitemap確認

```bash
curl -s https://ehime-hojokin.jp/sitemap.xml | grep "search?keyword"
curl -s https://ehime-hojokin.jp/sitemap.xml | grep "search_term_string"
curl -s https://ehime-hojokin.jp/sitemap.xml | grep "/features"
curl -s https://ehime-hojokin.jp/sitemap.xml | grep "/feature/"
curl -s https://ehime-hojokin.jp/sitemap.xml | grep "/subsidy/1570"
curl -s https://ehime-hojokin.jp/sitemap.xml | grep "/subsidy/1219"
curl -s https://ehime-hojokin.jp/sitemap.xml | grep -c "<url>"
```

結果:

- `/search?keyword=...`: 0件
- `search_term_string`: 0件
- `/features`: あり
- `/feature/...`: あり
- `/subsidy/1570`: あり
- `/subsidy/1219`: なし
- `<url>`: 464件

#### robots.txt確認

`robots.txt` では `/search` をブロックしていない。

確認結果:

- `User-agent: *`
- `Allow: /`
- `Disallow: /admin`
- `Disallow: /login`
- `Disallow: /dashboard`
- `Sitemap: https://ehime-hojokin.jp/sitemap.xml`

#### /search の noindex確認

`curl` で取得できる初期HTMLには、React SPAの仕様上、ルート別の動的metaは出ない。

本番配信済みJSには以下が含まれることを確認した。

```text
noindex,follow
```

つまり、現行実装では `/search?keyword=...` の `noindex,follow` はJavaScript実行後DOMで出る設計。

Search Console の「URL検査」→「公開URLをテスト」でレンダリング後HTMLを確認すると、`noindex,follow` が検出される想定。

#### 補助金詳細 title / description確認

`curl` の初期HTMLではSPA共通のtitle / descriptionが返る。

```text
title: 愛媛の補助金・助成金ポータル｜愛媛県内の事業者向け支援制度を検索
description: 愛媛県内の事業者向け補助金・助成金情報を検索できるポータルサイトです。
```

本番配信済みJSには、補助金詳細の公式情報確認ボックス文言が含まれることを確認した。

```text
申請前に確認したい公式情報
```

補助金詳細の個別title / descriptionもReact側で生成される設計のため、初期HTMLだけでは確認できない。Search Consoleのレンダリング後HTML、またはブラウザDOM確認が必要。

### SEO上の判断

- `/search?keyword=...` はrobots.txtでブロックしない。Googleがページをクロールできる状態にして、レンダリング後の `noindex,follow` を認識させる方針。
- sitemapから検索結果URLを除外し、正規にインデックスさせたいURLだけを残した。
- SPAのため、動的metaは初期HTMLではなくJS実行後DOMに出る。Googlebotは基本的にJSレンダリングを行うが、反映確認はSearch Consoleの「公開URLをテスト」で行うのが確実。

### 未対応事項

- `/search` の `noindex` をHTTPレベルでより確実にするなら、VPSのnginxで `/search` のみに `X-Robots-Tag: noindex, follow` を追加する余地がある。
- ただし、トップページ、補助金詳細、コラム、特集ページには絶対に `noindex` を付けないよう、nginx location設計は慎重に行う必要がある。
- 補助金詳細の個別title / descriptionはSPAのJS実行後に出るため、curlだけでは完全確認できない。今回はBrowser確認を行わない条件のため、Search Console URL検査で確認する。

### Search Consoleで次にやること

- `sitemap.xml` を再送信する。
- 以下のURLでURL検査を実施する。
  - `https://ehime-hojokin.jp/`
  - `https://ehime-hojokin.jp/search?keyword=松山市`
  - `https://ehime-hojokin.jp/search?keyword=製造業`
  - `https://ehime-hojokin.jp/subsidy/1570`
  - `https://ehime-hojokin.jp/features`
- `/search?keyword=...` では「noindexタグにより除外」方向へ変わるか確認する。
- `/subsidy/1570` ではレンダリング後HTMLのtitle / descriptionが個別化されているか確認する。

## 2026-05-29 追記: SEO第2フェーズの入口整備

Search Consoleで表示回数が増えている市町村名・給付金系クエリを、検索結果URLではなく固定LPと基礎コラムで受け止める方針にした。

実施内容:

- 地域別LPを追加: `/area/uwajima`, `/area/seiyo`, `/area/yawatahama`, `/area/saijo`
- 既存地域LPの title / description に「給付金」を追加
- 目的別LPを追加: `/purpose/benefits`, `/purpose/childcare`, `/purpose/housing`
- 基礎コラムを追加: `/column/kyufukin-hojokin-joseikin-chigai`, `/column/ehime-subsidy-application-first-step`, `/column/subsidy-official-info-checkpoints`

詳細な判断理由と次の運用手順は `docs/seo-growth-2026-05-29.md` に記録した。
